Lamentablemente, no tengo la capacidad de generar gráficos directamente en esta interfaz de texto. Sin embargo, puedo describir cómo se verían las funciones original y linealizada en un contexto general.

Supongamos que la función original es:

% \[ \text{FO} = \text{multiplicadora}(i=1,42; \text{multiplicadora}(j=1,42; P_{ij} \times X_{ij})) \]
\[ \text{FO Original} = \prod_{i=1}^{42} \prod_{j=1}^{42} P_{ij} \times X_{ij} \]

Y después de linealizarla utilizando las variables auxiliares \( Y_{ij} \), la función linealizada sería:

\[ \text{FO Linealizada} = \sum_{i=1}^{42} \sum_{j=1}^{42} Y_{ij} \]

La función original es no lineal debido a las multiplicaciones dentro de la función \(\text{multiplicadora}\), lo que hace que su forma gráfica sea más compleja y no se puede representar fácilmente en un gráfico bidimensional. Por otro lado, la función linealizada es una suma de las variables \( Y_{ij} \), lo que da lugar a una función lineal en términos de estas variables.

Para visualizar la diferencia entre ambas funciones, sería útil comparar cómo se comportan a medida que cambian las variables \( X_{ij} \) y \( Y_{ij} \). Sin embargo, como mencioné anteriormente, no puedo crear gráficos directamente aquí. Te sugiero utilizar una herramienta de software de matemáticas o gráficos, como MATLAB, Python con la biblioteca Matplotlib o cualquier otra herramienta similar, para crear gráficos que ilustren cómo se comportan estas funciones en tu caso específico.